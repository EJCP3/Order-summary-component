{
  "name": "order-summary-component",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "homepage": "https://EJCP3.github.io/Order-summary-component",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scanTa": "npx tailwindcss -i ./src/css/config.css -o ./src/css/estilos.css --watch",
    "build": "cp src/index.html build/ && cp -R src/css build/ && cp -R src/img build/",
    "predeploy": "npm run build",
     "deploy": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "tailwindcss": "^3.4.3"
  }
}
